[build]
builder = "nixpacks"

[deploy]
startCommand = "bunx prisma db push --accept-data-loss && next start -p ${PORT:-3000}"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
