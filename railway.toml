[build]
buildCommand = "bunx prisma generate && bun run build"

[deploy]
startCommand = "bunx prisma db push --accept-data-loss && bun run start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
